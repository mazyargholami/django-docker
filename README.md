# django-docker
Deploying django with docker compose

### Requirements
`Django
psycopg2
uWSGI`

### System:
`docker-compose up`

port: 8000

### Server:
`docker-compose -f docker-compse-deploy.yml up`

port: 80
